specVersion: "v1"
serviceId: "task"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "task-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
